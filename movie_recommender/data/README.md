# Data folder

Place `movies.csv` here (the TMDB 5000 Movie Dataset).

Download link: https://www.kaggle.com/datasets/tmdb/tmdb-movie-metadata

Required columns: title, genres, keywords, tagline, cast, director
(an `index` column is optional — the code auto-generates one if missing).
